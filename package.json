{
  "name": "my-app",
  "scripts": {
    "dev": "nuxt",
    "build": "nuxt build",
    "generate": "nuxt generate",
    "start": "nuxt start",
    "deploy": "push-dir --dir=dist --branch=gh-pages --cleanup"
  },
  "dependencies": {
    "fibers": "^5.0.0",
    "nuxt": "^2.14.8",
    "sass": "^1.29.0",
    "sass-loader": "^10.1.0",
    "vue-horizontal-scroll": "^0.2.4",
    "vuex": "^3.6.0"
  },
  "devDependencies": {
    "push-dir": "^0.4.1"
  }
}
